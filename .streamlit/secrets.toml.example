# Streamlit Cloud: Dashboard -> Settings -> Secrets (paste below, use your own PIN)
# Local testing: copy this file to secrets.toml in this folder (do not commit secrets.toml)

app_password = "1234"

# Set true ONLY for local dev if you do not want a PIN on your laptop:
# disable_auth = true
